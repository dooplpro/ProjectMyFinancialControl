import { describe, it, expect } from 'vitest'
import { screen, render } from '@testing-library/react'
import RecoverPage from './RecoverPage'

describe('Test to RecoverPage', () => {
  it('renders the RecoverPage component', () => {
    render(<RecoverPage />)
  })
})
